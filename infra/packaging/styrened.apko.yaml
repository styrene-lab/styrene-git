contents:
  repositories:
    - https://packages.wolfi.dev/os
    - '@local /work/artifacts/packages'
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    - /work/artifacts/packages/local-melange.rsa.pub
  packages:
    - bash
    - busybox
    - ca-certificates-bundle
    - jq
    - styrened-git-lab@local

entrypoint:
  command: /usr/bin/styrened

environment:
  PATH: /usr/sbin:/sbin:/usr/bin:/bin

archs:
  - amd64
  - arm64
